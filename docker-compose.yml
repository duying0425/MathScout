services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: mathscout
      POSTGRES_PASSWORD: mathscout
      POSTGRES_DB: mathscout
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
